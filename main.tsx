import React from 'react';
import ReactDOM from 'react-dom/client';
 import './vab.css'; // Optional: Ensure CSS imports if necessary
//import './index.css'; // Optional: Ensure CSS imports if necessary
import App from './app';

ReactDOM.createRoot(document.getElementById('root') !).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
